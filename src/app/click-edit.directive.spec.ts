import { ClickEditDirective } from './click-edit.directive';

describe('ClickEditDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEditDirective();
    expect(directive).toBeTruthy();
  });
});
